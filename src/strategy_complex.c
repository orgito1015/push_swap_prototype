#include "push_swap.h"

void	run_complex(t_env *env)
{
	/* TODO: implement O(n*log(n)) radix strategy on index-compressed values.
	   For now, this simply calls the simple strategy. */
	run_simple(env);
}
